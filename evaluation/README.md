# Evaluation entry points

This directory contains the released experiment scripts.

- `offline/run_offline_eval.py`
  Offline audit entry point.
- `online/run_agentdojo_benchmark.py`
  Online AgentDojo benchmark entry point.

These scripts are kept as thin public entry points around the current RISA modules.
